import { atom } from "recoil";

// export const loginRedirect = atom({
//     key: "loginRedirect",
//     default: false 
// })

export const sectionRedirect = atom({
    key:"sectionRedirect",
    default: "All Bounties"
})